# Legacy Deployment Orchestration

`run_server_experiments.sh` is retained only to reproduce the old Qwen2.5-era phase runner. It must not be used for current deployment or V4 research.

Use the V4 gate and the dedicated R1V4, R2, R3 and R4 scripts documented in `docs/research/KISAKI_EXPERIMENT_INDEX.md`.
