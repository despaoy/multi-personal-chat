[tool.ruff]
# Production uses Python 3.12, while the lab embedding environment still
# imports these modules on Python 3.10. Keep automated rewrites compatible.
target-version = "py310"
line-length = 120
required-version = ">=0.16.4"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "G",    # flake8-logging-format
    "SIM",  # flake8-simplify
    "TCH",  # flake8-type-checking
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "B008",   # do not perform function calls in argument defaults
    "B905",   # zip without strict
    "SIM108", # ternary operator (readability preference)
]

[tool.ruff.lint.isort]
known-first-party = ["api", "app", "bot", "cache", "character", "db", "infra", "inference", "knowledge", "middleware", "training", "interfaces"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "-p no:cacheprovider"
testpaths = ["tests"]
