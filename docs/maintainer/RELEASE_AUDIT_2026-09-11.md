# 发布前源码审查：2026-09-11

## 结论与边界

后续使用目标已明确为本地学习：授权和对外发布验收不阻碍本地逻辑检查，也未据此关闭现有安全校验。
后续逻辑修正包括训练参数别名/小数轮数、JSONL选择、训练取消及重启互斥、历史任务保留顺序、缺关系索引时的RAG降级。
本地逻辑修正后的全量回归：1802 passed、12 skipped、4条第三方弃用警告；未运行真实GPU训练或平台实测。
这覆盖本次工作区，不保证模型输出正确或不存在尚未覆盖的边界问题。

本次为本地工作区审查，不是已发布提交或生产环境验收。已修正可确认的源码、配置、
测试契约和发布排除规则问题；仍不能标记为“全部内容达到正式发布水平”。
发布前必须解决下方授权、依赖漏洞审计和部署验收事项。

## 覆盖范围

- 通过 Git 枚举全部跟踪文件和未被忽略的新文件，扫描约1,400个文件；不遍历外部依赖与本机运行环境。
- 全部拟发布Python源文件做AST语法解析，JSON/JSONL做格式解析；所有文件做字节哈希比对，检查完全相同的副本。
- 检查活动代码入口、容器与CI配置、公开README、安全边界、全后端静态规则、前端类型与构建、后端测试。
- 文档专项审查见[文档维护记录](DOCUMENTATION_REVIEW_2026-09-11.md)。
- 这不等于逐行证明每份历史报告正确，也不等于完整的渗透测试、密钥扫描、许可证审计或真实模型评测。
- 任务开始前已有的记忆实现和数据库修改保留，测试包含这些工作区内容；本次没有替用户提交或推送。

## 已处理问题

| 问题 | 处理 |
| --- | --- |
| 前端构建上下文可能含根目录数据库、缓存、测试运行环境、个人资料 | 补齐`.dockerignore`；后端同步排除环境变体、数据库旁路文件、测试目录与生成索引 |
| 本机测试运行环境及个人对照报告可能误入Git | 补齐`.gitignore`；原文件仍在本地 |
| 包管理配置关闭完整性校验、使用特定镜像源 | 恢复完整性校验，默认官方npm源；未升级依赖或改动锁文件 |
| Next.js残留未使用的旧平台开发域名和图片源 | 移除；当前源码未使用该图片源 |
| 旧工具发送文件时引用不存在的`desktop`变量 | 改用已计算的`send_dir` |
| 训练配置类型注解没有类型导入 | 增加仅类型检查导入，保持重型训练模块延迟加载 |
| 旧Bot重复导入命令注册函数 | 移除局部冗余导入 |
| 鉴权契约测试遗漏发送回执接口 | 登记其集成鉴权边界，并新增缺失/错误令牌、缺失签名拒绝测试 |
| 本地验证脚本错误宣称Alembic没有模块入口、缺依赖时跳过 | 统一选定Python执行Alembic，失败即停止 |
| 验证脚本清理共用测试目录 | 只清理本次运行自己的临时目录 |
| README混淆代码许可与游戏语料授权、历史记录只在Git的说法过时 | 澄清数据许可边界与历史归档用途 |

新增`check_release_hygiene.py`及回归测试，并接入CI和本地验证；CI同时增加未定义与重复Python名称检查。

## 冗余内容的处理原则

完全相同的文件主要包括包初始化文件、各批次空审核结果、原始与审核后未变化的候选数据。
这些文件有独立路径和流程含义，部分被清单、哈希或报告引用，不做自动删除或合并。
P6、旧实验、负结果及兼容入口仍保留原有归档/兼容边界。缓存、数据库和个人报告通过排除规则
隔离，不删除用户本地数据。容器排除规则不等于Git历史清理。

## 本次验证

- 前端TypeScript、ESLint通过；移除旧域名配置后的Next.js生产构建通过。
- 仓库完整性、发布文件语法/产物检查通过。
- RAG范围Ruff检查及格式检查通过；活动Python未定义/重复名称检查通过（历史脚本归档不纳入该规则）。
- Python依赖`pip check`通过；Alembic唯一head为`008_integration_receipts`。
- CI/Compose/工作区YAML语法与本地PowerShell验证脚本语法通过。
- 回执鉴权、架构契约与新增文件检查定向回归：41 passed。
- 全量后端回归最终结果见[发布检查表](../RELEASE_CHECKLIST.md)，避免重复维护计数。
- 沙箱内初次测试遇到临时目录权限错误，构建遇到子进程EPERM；最终验证使用独立测试数据库并在获准环境重跑。
- 本机pnpm命令出现`fetch failed`。前端检查与构建通过已安装的本地入口执行，不能据此宣称全新环境安装或在线漏洞审计通过。

## 发布前仍需完成

1. **数据许可**：确认游戏原文、衍生训练/RAG语料、素材和模型的再分发权限，或准备不含未授权资料的代码发行物。
   仅添加忽略规则或MIT声明不能解决已经跟踪资料的授权问题；本次不擅自改写历史或删除证据。
2. **全量规范债务**：全后端Ruff扫描仍有约2,300项报告，主要为旧类型注解、导入、日志写法和格式，也含需要人工判断的警告。
   RAG检查通过不代表全库规则通过；未用全局忽略或批量不安全修复掩盖这些问题。
3. **依赖安全**：在可联网环境完成锁文件的干净安装、前后端漏洞审计。`pip check`仅证明已安装依赖版本相容，不证明无漏洞。
4. **部署验收**：实际运行Docker镜像、PostgreSQL迁移、Redis、vLLM/GPU、AstrBot发送确认与失败恢复；本机无Docker工具，未执行容器验收。
5. **模型质量**：当前没有通过正式质量门禁的LoRA adapter。研究原型可以说明该限制，但不能作为已达标人物模型发布。
6. **工具执行隔离**：旧NoneBot工具和HTTP Claw必须保持受信任管理与隔离边界；子进程和AST检查不是操作系统沙箱。

必须在最终拟发布提交上重新运行CI及外部验收，不能复用本工作区结果作为未来修改的保证。
