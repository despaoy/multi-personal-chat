# 下一轮固定对照计划

末轮状态更新：种子组、分层检索和首轮最终回复均已完成，下文保留事前计划和历史进度；以阶段实测汇总为准。状态 v2 的感谢词表、完整输入预算、行为定义修复已实现，相关回归 75 条通过，正按预定顺序进行贪心与推理 seed42 对照。公开题 6761 已完成双组带原始尝试的定向诊断：兜底交付修复生效，但未改善原模型回答，不升级默认配置。

最终收尾更新：v2 贪心和推理 seed42 以及 v2 最终回复 36 条全部完成，结果见实测汇总；本页下方的“下一项/正在运行”为历史计划，不代表仍有进程运行。最新全后端 2086 passed / 12 skipped。下一阶段按 `CONTEXTUAL_EVIDENCE_HANDOFF.md` 先做独立终测与人工评审，不继续在同一 12 题上追逐分数。

2026-09-19，本地执行计划，不是外部预注册。已知结果：直接非推理贪心 19/24；推理采样 seed=42 为 23/24。四维与两阶段协议未稳定胜出。所有开发题的金标和原报告保留。

## 解码与推理分离

先固定 direct 选择协议、模型修订、底座 NF4、候选召回和当前提示，再比较：

1. 非推理贪心：已有完整挑战与迁移报告。
2. 非推理采样：温度 0.7、top-p 0.8、top-k 20、min-p 0，种子 42、43、44。
3. 推理采样：温度 0.6、top-p 0.95、top-k 20、min-p 0，种子 42、43、44。

两个采样配方来自模型卡，不是同温度的单因素消融。因此即使第三组胜出，也只能归因于“推理及其推荐解码配方”组合；不能单独归因于思考标记。温度/预算匹配的进一步消融另做，不冒称已完成。

先跑完整 24 条挑战，随后按同一固定种子组跑 16 条迁移探针。seed=42 推理挑战已完成，迁移正在运行；不因后续某种子变差而删除该结果或选择性汇报。报告逐种子分数、全失败题和跨种子不稳定题，不把重复采样当成新增独立问题。

不在过程中继续修改提示和金标；若必须修复影响输入的程序错误，保存旧版本，单独记录新轮，不能混并。比较器检查每轮数据哈希。更早运行的代码有上游证据保存修复差异，但这些固定样例无第五条证据/来源链接，因此候选和提示并未受该修复影响。

## 状态估计的真实入口

人设策略先前使用人工供给的状态提示。新增 `--state-source rules` 和 `--state-source semantic`，让现有规则分析器/选择性语义估计器实际处理输入。预期行为仅留在评测器，不传给模型。分别报告状态回退、原策略投影约束与语义策略投影约束，避免把上游识别失败错误归因给策略模型。

三组已完成，详细分母和限制见 `CONTEXTUAL_EVIDENCE_RESULTS_20260919.md`。全量复核比选择性覆盖更多漏检，但仍有错误，感谢标签也不在可选集合；不得将它直接推广为默认。下一项固定试验仅隔离状态估计，不再为两个角色重复调用策略模型：`scripts/evaluate_semantic_state.py --thinking --seed 42`。保留同一 12 题、同一状态提示和解析器，比较推理推荐配方；它同时改变解码参数和输出预算，非单因素消融。默认不保存模型最终原文，可显式开启诊断；从不保存推理草稿。先完成正在运行的最终回复对照，再运行此项，不并发占用 GPU。

## 公开检索的分层表示诊断

70 条固定抽样完成后，RRF 会话召回为 0.966667，轮次定位召回为 0.741667；尤其偏好类仅 0.383333。下一轮保留同样 70 条、同样分块/嵌入/BM25/RRF 配方，新增两种事后假设：先取 RRF 前 5 个会话，在其中按 MiniLM 排序轮次；或按 RRF 会话顺序轮流取每个会话的 MiniLM 高位轮次。不使用金标决定会话、排序或预算。两者是待验证的分层策略，不冒称学习模型。

各组仍比较 top-5 去重轮次；另外在相同 2048-token 证据预算下整块装入每轮最高排名片段，记录实际 token 消耗及轮次召回。这里 token 来自 MiniLM 分词器，不是生成模型分词器；命中带答案的轮次不保证选中片段包含答案，不能写成最终 QA 提升。不覆盖旧报告，不因某个方案变差而只保留较好方案；未来仍需未参与设计的独立子集。

## 晋升前不能省略的事项

源码审查另外发现两个待修复的契约问题，先保留当前生成批次，不在中途修改：一是正常感谢 `gratitude` 被排除在语义模型的可选行为之外，模型即便理解也不能合法表达；二是状态复核的当前消息和历史采用前缀截断，可能丢掉句末否定/纠正。计划在当前批次完成后扩展合法的非安全感谢标签，并改成完整输入预算校验，放不下则显式回退原状态，不假装复核了完整语义。安全与风险解除标签的独立保护不变，不追加关键词匹配规则。新增对应回归后另写报告，旧模型报告不覆盖；这些是接口完整性修复，不预先宣称模型质量提升。

状态推理 v1 已完成，必要行为缺失 7/12，并未优于非推理全量的 6/12；9/12 情景匹配也不能代替行为或回答质量。下一版在补齐感谢标签外，只澄清闭集行为定义：区分对当前对话者的互动行为与谈论的外部事件，允许同轮行为并存，不提供样例原句或预期答案，不根据分数强行抬高已观察题的标签。不增加词面分支。冻结后先跑完整 12 题的非推理贪心，再跑相同 seed42 推理配方；属于开发诊断的组合协议更新，不能做单因素因果或独立泛化结论。原状态模块逻辑已保存到 `source_patches/semantic_state_v1_20260919.patch`。

追加独立的输入表示实验（在看到 seed=42 历史漏选之后提出，不能当成独立验证）：原始视图之外，`status_neutral` 仅隐藏存储状态标签，保留原文、完整证据、日期及 historical；`interval_prose` 在前者基础上增加日期范围的中文说明，不解析查询、不强制任何标签。上游授权和生命周期过滤不变，删除/撤回/待确认条目不能经此视图复活。分别用非推理贪心与推理 seed=42 跑完整挑战/迁移题；这两个视图先固定，不根据中途结果继续改写。入口 `scripts/evaluate_memory_metadata_view.py`，仍未改变生产选择器。种子汇总拒绝把不同候选视图混为同一配方。

最终回复试验：`scripts/evaluate_contextual_replies.py` 通过现有上下文/生成/输出守卫链路生成匿名 A/B/C，三组依次为规则、全量非安全状态复核、全量复核加情境策略。已有 12 条合成题×3 组真实生成完成，人物固定月社妃，所有组使用相同底座、人物提示、贪心非推理配方。无 RAG/记忆/LoRA，不声称全系统比较；人工原作一致性及体验评审未完成。注意首轮关系夹具字段错误，实际为 stranger，非此前声称的 familiar；详见阶段结果更正，原报告不覆盖。

执行记录：非推理采样挑战 seed=42/43/44 已完成，均为 19/24，五个错误题完全一致。迁移 seed=42/43/44 也完成，均为 11/16，五个错误题一致。顺序批处理在最后的迁移 seed=44 未生成完整报告时停止，原因未确定；保持全部参数与种子不变单独重试后成功（退出码 0）。此失败尝试不算通过，也未删除记录。推理 seed=43/44 的两组数据集对照已启动，结果未定。

追加执行记录：推理挑战 seed=43 已完成，23/24，但不是 seed=42 的同一错误：历史题 `temporal_02` 通过，多证据题 `multi_02` 在 120 秒/717 token 时未完成而回退，TP=12/FP=0/FN=2、服务失败1。保留原失败，不重跑挑成功。尚不能说历史错判跨所有种子稳定；时间表示实验仍只作为另行标注的事后诊断。

这些本机运行同时存在 CPU 检索评测，系统负载和吞吐并未严格控制；超时是实际运行失败，但不能仅归因于随机种子或模型语义能力。后续应在独占资源及固定 token/时间预算下另做时延/完成率复测，不能覆盖本次结果。

推理挑战三种子已完成：23/24、23/24、22/24；同时正确仅 20/24。seed=44 的 `reference_03` 超时、`injection_01` 错误注入。全部三种子的严格多数回放为 24/24、TP14/FP0/FN0，但需要合计 63 次模型调用、2732.862 秒生成时间。这是事后开发集回放，不是已验证上线方案；不能将少数种子的错误和服务失败从原报告抹去。非推理多数回放仍为挑战 19/24、迁移 11/16，未带来改善。

公开中文最终回复试验也已准备：`scripts/evaluate_public_character_replies.py`，固定 CharacterEval 修订和 8 角色×2 题，与冻结适配审计相同 16 个 ID。规则对全量非安全语义复核，两组均走现有服务、提示和守卫。它独立于本项目 AI 编写的开发题，但模型预训练污染未知；没有官方 CharacterRM 或人工评分，不能单凭生成完成宣称质量提高。原文不复制到版本库报告，按外部缓存 ID 审阅。

推理迁移三种子已全部完成：15/16、16/16、15/16，无服务失败；seed=44 在翻译题 `task_translate` 错误调用个人记忆，seed=42 漏掉历史题 `history_old`。同时正确 14/16。全部三种子的严格多数回放为 16/16、8/8 成对通过，合计 48 次模型调用、1603.545 秒生成时间。报告 `contextual_memory_thinking_transfer_seed_summary_20260919.json`。这仍是基于已知错误类型编写的诊断集，不能据此直接启用生产功能。

事后多数表决固定用全部 42/43/44，严格两票才能选中；失败不当作有效否定，少于两次有效运行记为失败。单题最多五条，按票数、平均原排序和 ID 打破并列。只比较固定回放，不挑选最好的两次，也不把同模型投票解释为独立验证或可信概率；额外推理成本必须报告。

- 以上均是 AI 编写的中文开发/诊断数据，不是正式独立终测。
- 仍需用户轨迹/原作场景分组的独立中文评估，以及最终回复盲审。
- NF4/Transformers 的质量、时延不自动等同于线上 AWQ/vLLM。
- 实验不会自动打开线上开关、导入真实记忆、训练或批准 LoRA。
